// Write all the code here
import React from 'react';
import Header from './layout/Header';
import Footer from './layout/Footer';

// Create a navigation bar with links to Home, Contact, About, shopping cart, and a menu with links to Login,
// Orders and Logout


const Home = () => {
    return (
      <div>
        <Header />
        <Footer />
      </div>
    );
  };

export default Home;
